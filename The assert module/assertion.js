var assert = require('assert') //imports the assert module in nodejs
assert(50 > 70) //throws an assertion error saying that assertions has been evaluated to a falsy value
assert(50 < 70) // returns true i.e. throws no errors
